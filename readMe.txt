1. Id returns unique id. While className, SelectorAll return array like object, and selector return first matching entity given.

2.
creat an element first via creatElement('x'), then add this element using appendChild to the location where it is needed.

3.it's work flow is from branch node to  root of DOM.

4. It is to handle event efficiently with addEventListener. It is usefull for its uses.

5.preventDefault() stops basic behaviour. Like if a buttun is added to a form and clicked on the button, resulting refresh the web page.To prevent this kind of behaviour we use this. 
stopPropagation() is use to stop bubbling effect.

